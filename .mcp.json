{
  "mcpServers": {
    "kevin": {
      "command": "bun",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/src/server.ts"],
      "env": {
        "AGENT_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}",
        "AGENT_PLUGIN_DATA": "${CLAUDE_PLUGIN_DATA}",
        "PLAYWRIGHT_BROWSERS_PATH": "0"
      }
    }
  }
}
